[project]
name = "user-management-agent"
version = "0.1.0"
description = "OpenAI Agent with Remote MCP Server"
requires-python = ">=3.11"

dependencies = [
    "openai-agents",
    "python-dotenv",
    "streamlit>=1.60.0",
    "streamlit-chat>=0.1.1",
]
